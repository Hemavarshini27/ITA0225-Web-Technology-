<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sample Web Page with Internal CSS</title>
    <style>
        /* Global Styles */
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
        }

        h1, h2, h3 {
            color: #333;
        }

        p {
            line-height: 1.6;
            color: #666;
        }

        /* Navigation Bar */
        nav {
            background-color: #333;
            color: white;
            padding: 10px 20px;
        }

        nav ul {
            list-style: none;
            margin: 0;
            padding: 0;
            display: flex;
        }

        nav ul li {
            margin-right: 20px;
        }

        nav ul li a {
            color: white;
            text-decoration: none;
        }

        nav ul li a:hover {
            text-decoration: underline;
        }

        /* Header Section */
        .header {
            background-color: #f4f4f4;
            padding: 20px;
            text-align: center;
        }

        .header h1 {
            margin-bottom: 10px;
        }

        .header p {
            margin-bottom: 0;
            font-size: 1.2em;
        }

        /* Slider Section */
        .slider-container {
            position: relative;
            max-width: 100%;
            margin: 20px auto;
            overflow: hidden;
        }

        .slides {
            display: flex;
            width: 500%;
            transition: transform 0.5s ease-in-out;
        }

        .slides img {
            width: 100%;
            height: 400px;
        }

        .radio-buttons {
            display: flex;
            justify-content: center;
            margin-top: 10px;
        }

        .radio-buttons input[type="radio"] {
            display: none;
        }

        .radio-buttons label {
            border: 2px solid #ddd;
            padding: 10px;
            border-radius: 50%;
            cursor: pointer;
            margin: 0 5px;
        }

        .radio-buttons input[type="radio"]:checked + label {
            background-color: #333;
            color: #fff;
            border-color: #333;
        }

        /* Content Section */
        .content {
            display: flex;
            justify-content: space-around;
            margin: 20px 10px;
        }

        .content div {
            width: 30%;
            background-color: #f9f9f9;
            padding: 20px;
            border: 1px solid #ddd;
        }

        .content div h2 {
            margin-bottom: 10px;
        }

        .content div p {
            font-size: 0.9em;
        }

        /* Footer Section */
        footer {
            background-color: #333;
            color: white;
            text-align: center;
            padding: 20px;
            margin-top: 20px;
        }

        footer p {
            margin: 0;
        }
    </style>
</head>
<body>

    <!-- Navigation Bar -->
    <nav>
        <ul>
            <li><a href="#">Home</a></li>
            <li><a href="#">About</a></li>
            <li><a href="#">Services</a></li>
            <li><a href="#">Contact</a></li>
        </ul>
    </nav>

    <!-- Header Section -->
    <div class="header">
        <h1>Welcome to My Web Page</h1>
        <p>Explore the content and enjoy the sliding images below</p>
    </div>

    <!-- Image Slider Section -->
    <div class="slider-container">
        <div class="slides" id="slides">
            <img src="https://via.placeholder.com/1200x400/FF5733/FFFFFF?text=Slide+1" alt="Slide 1">
            <img src="https://via.placeholder.com/1200x400/33FF57/FFFFFF?text
